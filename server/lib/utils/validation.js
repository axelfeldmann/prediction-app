export const validateUsername = (username) => {
  const validationResult = { success: false, message: '' };
};

export const validatePassword = (password) => {
  const validationResult = { success: false, message: '' };
};

export const validateLeagueName = (leagueName) => {
  const validationResult = { success: false, message: '' };
};